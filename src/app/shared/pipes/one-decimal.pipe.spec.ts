import { OneDecimalPipe } from './one-decimal.pipe';

describe('OneDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new OneDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
