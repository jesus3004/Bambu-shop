import { NoneDecimalPipe } from './none-decimal.pipe';

describe('NoneDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new NoneDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
